import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivai.voicewidget',
  appName: 'ShivAI Voice Widget',
  webDir: 'dist'
};

export default config;
